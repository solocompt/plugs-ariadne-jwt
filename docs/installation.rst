============
Installation
============

At the command line::

    $ easy_install django-ariadne-jwt

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-ariadne-jwt
    $ pip install django-ariadne-jwt
